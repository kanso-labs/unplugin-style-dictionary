{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "security:minimumReleaseAgeNpm is extended directly rather than through config:best-practices, which also pulls in helpers:pinGitHubActionDigests, :configMigration, :pinDevDependencies, :maintainLockFilesWeekly, abandonments:recommended and docker:pinDigests. The first of those contradicts the convention that actions are pinned to exact release tags, which is shared across the kanso-labs repositories, and the rest are behaviours this repository has not opted into. Extending the security preset directly keeps its own null opt-outs for lockFileMaintenance, replacement, pin, bump, lockfileUpdate and rollback, which is what a hand-written top-level minimumReleaseAge would lose: those update types carry no release timestamp, so a global value raises pull requests that can never merge, and this repository automerges lockFileMaintenance.",
  "extends": ["config:recommended", "security:minimumReleaseAgeNpm"],
  "labels": ["area:packaging", "kind:tooling"],
  "lockFileMaintenance": {
    "automerge": true,
    "enabled": true
  },
  "mise": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Renovate's own config:recommended sets semanticCommitType through a packageRule, and packageRules beat top-level config. Setting it at the top level therefore does nothing at all: every upgrade stays chore and cuts no release.",
      "matchPackageNames": ["*"],
      "semanticCommitType": "deps"
    },
    {
      "automerge": true,
      "matchUpdateTypes": ["minor", "patch"]
    },
    {
      "groupName": "commitlint",
      "matchPackageNames": ["@commitlint/**"]
    },
    {
      "groupName": "eslint",
      "matchPackageNames": [
        "@eslint/**",
        "eslint",
        "eslint-config-**",
        "eslint-plugin-**",
        "typescript-eslint",
        "!eslint-plugin-oxlint"
      ]
    },
    {
      "description": "eslint-plugin-oxlint declares a hard `~` peer on the matching oxlint minor, so a lone bump of either resolves to ERESOLVE. The eslint rule above negates the plugin out of its `eslint-plugin-**` matcher so this pairing does not rest on rule order.",
      "groupName": "oxlint",
      "matchPackageNames": ["eslint-plugin-oxlint", "oxlint"]
    },
    {
      "groupName": "vitest",
      "matchPackageNames": ["@vitest/**", "vitest"]
    }
  ],
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "rebaseWhen": "behind-base-branch",
  "recreateWhen": "always",
  "semanticCommitScope": null,
  "semanticCommits": "enabled"
}
